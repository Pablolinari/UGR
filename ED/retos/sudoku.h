#include "celdas.h"
struct pos {
    int x;
    int y;
};
class sudoku{
    private:
    Celdas sudoku[9][9];
    
    public:
    /*
    Devuelve la casilla con el num de celdas mas pequenio que este sin rellenar . , ejemplo : si el sudoku tiene una sola casilla 
    rodeada elige esa ya que en esa casilla solo puede ir un numero , si no hay de uno busca de dos , tres etc.

    */
    pos MasPequenia()
    
    

};